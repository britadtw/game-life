# Game Life UI 重新設計

## 改動概要

### 前台 (使用者介面) - `/`

#### 主要特點：
1. **互動式目標卡片**
   - 使用 `GoalCard` 組件，每個目標一張大卡片
   - 漸層色彩頭部顯示目標資訊、指派人員和總積分
   - 進度條顯示每日/每週任務完成狀況
   
2. **分頁式任務檢視**
   - 切換檢視每日任務和每週任務
   - 點擊任務即可標記完成，無需進入後台
   - 樂觀更新 (Optimistic UI) 提供即時反饋

3. **視覺化獎勵系統**
   - 已達成獎勵以金色漸層顯示，並有特殊圖標
   - 未達成獎勵顯示為灰色鎖定狀態

4. **響應式設計**
   - 支援手機、平板和桌面裝置
   - 使用 Tailwind CSS 和 dark mode

### 後台 (管理介面) - `/admin/*`

#### 架構重組：
將原本單一頁面的後台拆分成多個功能頁面：

1. **概覽頁** - `/admin`
   - 顯示所有目標的操作面板
   - 快速完成任務和目標
   - 查看目標狀態和進度

2. **人員管理** - `/admin/people`
   - 列出所有參與者
   - 顯示每個人員的目標數量
   - 新增人員表單

3. **目標管理** - `/admin/goals`
   - 列出所有目標及其詳細資訊
   - 顯示目標的任務數和獎勵數
   - 建立新目標（包含日期、週期設定）

4. **任務管理** - `/admin/tasks`
   - 分類顯示每日任務和每週任務
   - 每個任務顯示所屬目標和指派人員
   - 建立新任務（選擇類型和積分）

5. **獎勵管理** - `/admin/rewards`
   - 分類顯示點數獎勵和完成獎勵
   - 建立新的點數門檻獎勵
   - 顯示獎勵所屬目標

#### 共用元件：
- **AdminLayout** - 側邊欄導航
  - 固定側邊欄，包含所有管理頁面連結
  - 高亮顯示當前頁面
  - 返回首頁按鈕

## 新增檔案

```
app/
├── actions.ts                      # 前台 action (完成任務)
├── components/
│   ├── GoalCard.tsx               # 前台目標卡片組件
│   └── AdminLayout.tsx            # 後台佈局組件
└── admin/
    ├── page.tsx                   # 概覽頁 (已修改)
    ├── people/
    │   └── page.tsx              # 人員管理頁 (新)
    ├── goals/
    │   └── page.tsx              # 目標管理頁 (新)
    ├── tasks/
    │   └── page.tsx              # 任務管理頁 (新)
    └── rewards/
        └── page.tsx              # 獎勵管理頁 (新)
```

## 設計特色

### 色彩系統
- **目標卡片**: 漸層紫粉色 (indigo → purple → pink)
- **每日任務**: 藍色系 (blue → indigo)
- **每週任務**: 紫色系 (purple → pink)
- **積分/獎勵**: 琥珀金色系 (amber → yellow)
- **完成狀態**: 綠色系 (green → emerald)

### 互動設計
- 懸停效果 (hover states)
- 陰影層次感
- 平滑過渡動畫
- 按鈕狀態反饋
- 樂觀更新 (Optimistic UI)

### 可用性改善
- 清晰的視覺層級
- 圖標輔助說明
- 提示和幫助訊息
- 表單驗證提示
- 響應式佈局

## 技術亮點

1. **Server Components + Client Components**
   - 頁面使用 Server Components 獲取資料
   - GoalCard 使用 Client Component 處理互動

2. **Optimistic Updates**
   - 任務完成時立即更新 UI
   - 失敗時自動回滾

3. **TypeScript 類型安全**
   - 完整的類型定義
   - Props 介面清晰

4. **無障礙設計**
   - 語義化 HTML
   - 適當的 ARIA 標籤
   - 鍵盤導航支援

## 使用方式

### 前台操作
1. 瀏覽首頁查看所有目標
2. 切換「每日任務」和「每週任務」分頁
3. 點擊任務旁的「完成」按鈕標記完成
4. 查看獎勵解鎖進度

### 後台管理
1. 從首頁點擊「管理」按鈕進入後台
2. 使用左側導航切換不同管理頁面：
   - **人員**: 新增參與者
   - **目標**: 建立新目標，設定日期和週期
   - **任務**: 為目標新增每日/每週任務
   - **獎勵**: 設定點數獎勵門檻
   - **概覽**: 操作任務和目標完成狀態
